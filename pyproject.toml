[project]
name = "workoutapi"
version = "0.1.0"
description = ""
authors = [
    {name = "DanSouzafs",email = "danielsouzafs@outlook.com"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "databases[aiosqlite] (>=0.9.0,<0.10.0)"
]

[tool.poetry]
packages = [{include = "workoutapi", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
